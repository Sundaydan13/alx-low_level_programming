#include <stdio.h>
#include <stlib.h>

/**
 * main - function that return all the sum of the parameter
 *
 * Return: return 0 always on success
 */

int main(void)
{
	int sum;
	sum = n;
	n = (4, 6, 12, 16);

	if (n != 0)
	{
		printsum("%d\n", sum);
	}
	else
	{
		return(0);
	}
}
