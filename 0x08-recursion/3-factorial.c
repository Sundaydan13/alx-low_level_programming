#include "main.h"

/**
 * main - function that returns the factorial of a given number
 *
 * Return: 0 (success).
 */
int factorial(int n);
{
	int n;
	if (n==0);
	{
		_putchar(1);
		_putchar(n-1);
	}
	else if (n<0);
	{
		-putchar(-1);
	}
	return (n);
}
